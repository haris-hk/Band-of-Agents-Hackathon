# Copy to agent_config.yaml and fill with rotated Band agent credentials.
# Never commit agent_config.yaml (it is gitignored).

alert_triager:
  agent_id: "your-alert-triager-agent-id"
  api_key: "band_a_your_key_here"
incident_reproducer:
  agent_id: "your-incident-reproducer-agent-id"
  api_key: "band_a_your_key_here"
patch_generator:
  agent_id: "your-patch-generator-agent-id"
  api_key: "band_a_your_key_here"
regression_test_generator:
  agent_id: "your-regression-test-generator-agent-id"
  api_key: "band_a_your_key_here"
rca_publisher:
  agent_id: "your-rca-publisher-agent-id"
  api_key: "band_a_your_key_here"
